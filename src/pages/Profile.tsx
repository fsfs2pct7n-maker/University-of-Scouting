export default function Profile() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: 'calc(100vh - 120px)',
      }}
    >
      <p style={{ fontSize: '16px', color: 'var(--gray)' }}>No items</p>
    </div>
  )
}
