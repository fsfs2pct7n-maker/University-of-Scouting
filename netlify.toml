[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — all routes handled by React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
